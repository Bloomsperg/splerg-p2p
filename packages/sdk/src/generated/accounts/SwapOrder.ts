/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';

/**
 * Arguments used to create {@link SwapOrder}
 * @category Accounts
 * @category generated
 */
export type SwapOrderArgs = {
  maker: web3.PublicKey;
  taker: web3.PublicKey;
  makerTokenMint: web3.PublicKey;
  takerTokenMint: web3.PublicKey;
  makerAmount: beet.bignum;
  takerAmount: beet.bignum;
  bump: number;
};
/**
 * Holds the data for the {@link SwapOrder} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class SwapOrder implements SwapOrderArgs {
  private constructor(
    readonly maker: web3.PublicKey,
    readonly taker: web3.PublicKey,
    readonly makerTokenMint: web3.PublicKey,
    readonly takerTokenMint: web3.PublicKey,
    readonly makerAmount: beet.bignum,
    readonly takerAmount: beet.bignum,
    readonly bump: number,
  ) {}

  /**
   * Creates a {@link SwapOrder} instance from the provided args.
   */
  static fromArgs(args: SwapOrderArgs) {
    return new SwapOrder(
      args.maker,
      args.taker,
      args.makerTokenMint,
      args.takerTokenMint,
      args.makerAmount,
      args.takerAmount,
      args.bump,
    );
  }

  /**
   * Deserializes the {@link SwapOrder} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo: web3.AccountInfo<Buffer>, offset = 0): [SwapOrder, number] {
    return SwapOrder.deserialize(accountInfo.data, offset);
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link SwapOrder} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<SwapOrder> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find SwapOrder account at ${address}`);
    }
    return SwapOrder.fromAccountInfo(accountInfo, 0)[0];
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey('GKTd9AGFpPGNKK28ncHeGGuT7rBJLzPxNjCUPKn8Yik8'),
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, swapOrderBeet);
  }

  /**
   * Deserializes the {@link SwapOrder} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [SwapOrder, number] {
    return swapOrderBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link SwapOrder} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return swapOrderBeet.serialize(this);
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link SwapOrder}
   */
  static get byteSize() {
    return swapOrderBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link SwapOrder} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(SwapOrder.byteSize, commitment);
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link SwapOrder} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === SwapOrder.byteSize;
  }

  /**
   * Returns a readable version of {@link SwapOrder} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      maker: this.maker.toBase58(),
      taker: this.taker.toBase58(),
      makerTokenMint: this.makerTokenMint.toBase58(),
      takerTokenMint: this.takerTokenMint.toBase58(),
      makerAmount: (() => {
        const x = <{ toNumber: () => number }>this.makerAmount;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      takerAmount: (() => {
        const x = <{ toNumber: () => number }>this.takerAmount;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      bump: this.bump,
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const swapOrderBeet = new beet.BeetStruct<SwapOrder, SwapOrderArgs>(
  [
    ['maker', beetSolana.publicKey],
    ['taker', beetSolana.publicKey],
    ['makerTokenMint', beetSolana.publicKey],
    ['takerTokenMint', beetSolana.publicKey],
    ['makerAmount', beet.u64],
    ['takerAmount', beet.u64],
    ['bump', beet.u8],
  ],
  SwapOrder.fromArgs,
  'SwapOrder',
);
